import PublicQuoteCatalog from "@/components/PublicQuoteCatalog";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Cotação — Apartamento GB",
};

export default function CotacaoPage() {
  return <PublicQuoteCatalog />;
}
